streamlit>=1.36
pandas>=2.0
openpyxl>=3.1
pypdf>=4.0
python-docx>=1.1
pydantic>=2.0
cryptography>=42.0  # cifragem de PII em repouso (LGPD)
anthropic>=0.40  # Parte 2 (IA real) — structured outputs via messages.parse()
