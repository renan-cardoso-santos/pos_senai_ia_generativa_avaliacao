streamlit>=1.36
pandas>=2.0
openpyxl>=3.1
pypdf>=4.0
python-docx>=1.1
pydantic>=2.0
cryptography>=42.0  # cifragem de PII em repouso (LGPD)
# Parte 2 (IA real) — descomente quando integrar o LLM:
# anthropic>=0.40
